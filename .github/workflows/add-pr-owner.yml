name: Dependabot

on:
 push:

jobs:
  dependabot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Run Dependabot
        uses: dependabot/dependabot-action@v3 # Use a ação oficial do Dependabot
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          directory: "/" 
    

      
        
