name: Push on commit

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Send Push Notification
      uses: techulus/push-github-action@1.0.0
      env:
        API_KEY: b5373e57-3ff5-4307-99bc-500a3b881987
        MESSAGE: "There is a new commit!"
