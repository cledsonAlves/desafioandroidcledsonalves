on:
  workflow_dispatch:
  schedule:
    - cron: '*/2 * * * *'


jobs:
  test_schedule:
    runs-on: ubuntu-latest
    steps:
  
      - name: Every time
        run: echo "This step will always run"
