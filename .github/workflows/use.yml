name: Boot CI

on:
  pull_request:
     types:
      - labeled


jobs:
  valida-label:
    uses: ./.github/workflows/validate_label.yml
    
    
  remove-label:
   runs-on: ubuntu-latest
   
   needs: valida-label
   steps:
    - run: echo  'Rodou todos os jobs'
    
  remove-label2:
   runs-on: ubuntu-latest
   
   needs: valida-label
   steps:
    - run: echo  'Rodou todos os jobs'
    
  remove-label3:
    runs-on: ubuntu-latest
   
    needs: valida-label
    steps:
    - run: echo  'Rodou todos os jobs'

  

    
    
