name: Reusable workflow with matrix strategy

on:
  pull_request:
     types:
      - labeled


jobs:
  call-workflow:
    uses: ./.github/workflows/use.yml

  call-workflow-passing-data:
    permissions:
      contents: read
      pull-requests: write
    uses: ./.github/workflows/use.yml
    with:
      config-path: .github/use.yml
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
    
